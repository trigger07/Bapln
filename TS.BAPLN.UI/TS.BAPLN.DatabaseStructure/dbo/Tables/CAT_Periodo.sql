CREATE TABLE [dbo].[CAT_Periodo] (
    [Id]          INT            IDENTITY (1, 1) NOT NULL,
    [Descripcion] NVARCHAR (200) NULL,
    CONSTRAINT [PK_CAT_Periodo] PRIMARY KEY CLUSTERED ([Id] ASC)
);

